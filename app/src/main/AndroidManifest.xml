<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="martin.sms">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Student.MainActivity" />
        <activity android:name=".Student.RegisterActivity" />
        <activity android:name=".RegisterTypes" />
        <activity android:name=".Professor.RegisterActivityProfessor" />
        <activity android:name=".Admin.RegisterActivityAdmin" />
        <activity android:name=".Admin.ProfileActivityAdmin" />
        <activity android:name=".Professor.ProfileActivityProfessor" />
        <activity android:name=".Professor.SubjectListActivity" />
        <activity android:name=".Professor.ProfessorEvidenceList" />
        <activity android:name=".Student.AllSubjectsList" />
        <activity android:name=".Student.StudentSubjectList" />
        <activity android:name=".Student.ProfileActivityStudent" />
        <activity android:name=".Professor.ProfessorAllSubjectList" />
        <activity android:name=".Professor.ProfessorAllSubjectListAdapter" />
        <activity android:name=".Professor.ProfessorSubjectList" />
        <activity android:name=".Admin.UpdateStudent" />
        <activity android:name=".Admin.AddSubject" />
        <activity android:name=".Admin.FindByIndex" />
        <activity android:name=".Admin.AddSubjectToStudent" />
        <activity android:name=".Admin.FindProfessorByEmail" />
        <activity android:name=".Admin.UpdateProfessor" />
        <activity android:name=".Admin.AddSubjectToProfessor" />
        <activity android:name=".Admin.FindSubjectByName" />
        <activity android:name=".Admin.UpdateSubject" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Student.MapsActivity"
            android:label="@string/title_activity_maps"></activity>
    </application>

</manifest>